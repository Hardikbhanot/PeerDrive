services:
  peerdrive:
    build:
      context: .
      dockerfile: Dockerfile
    image: peerdrive:local
    container_name: peerdrive
    ports:
      - "8081:8080"
    environment:
      PEERDRIVE_PUBLIC_URL: ${PEERDRIVE_PUBLIC_URL:-http://localhost:8081}
    volumes:
      - peerdrive_data:/data
    restart: unless-stopped

volumes:
  peerdrive_data:
